---
phase: 07-content-debranding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js
autonomous: true
requirements:
  - ONBD-01
must_haves:
  truths:
    - "The onboarding CLI replaces 'Impulse English Academy' and 'Impulse English' in all 23 markdown article files with the new client's brand name during onboarding"
    - "Markdown articles are backed up to a timestamped directory before modification"
    - "The replacement runs after config file promotion and before build verification"
    - "readdirSync is imported and used to scan src/content/articles/ for .md files"
    - "The onboarding CLI scans .astro page meta tags for residual hardcoded brand references and replaces them as a belt-and-suspenders safety net (per D-10)"
  artifacts:
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js"
      provides: "replaceInMarkdownArticles + replaceInAstroPageMeta functions, readdirSync import, call sites after promotion"
      contains: "replaceInMarkdownArticles"
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js"
      provides: "Astro meta tag safety-net replacement function"
      contains: "replaceInAstroPageMeta"
  key_links:
    - from: "scripts/onboard-client.js"
      to: "src/content/articles/*.md"
      via: "readdirSync + readFileSync + writeFileSync"
      pattern: "readdirSync.*content.*articles"
    - from: "scripts/onboard-client.js"
      to: "src/pages/**/*.astro"
      via: "recursive scan + readFileSync + writeFileSync"
      pattern: "replaceInAstroPageMeta"
---

<objective>
Add markdown article debranding and .astro meta tag safety-net steps to the onboarding CLI so that when a new client runs `npm run onboard`, all 23 markdown article files and any residual .astro meta hardcoded brand references are replaced with the new client's brand name.

Purpose: Closes the markdown portion of gap HIGH-08 per D-09, and adds a belt-and-suspenders .astro meta sweep per D-10. Without this, a cloned template would still contain "Impulse" in 23 article files that YAML frontmatter cannot import from TypeScript. The CLI string replacement is the only mechanism to debrand these files for new clients.

Output: Updated onboard-client.js with replaceInMarkdownArticles(), replaceInAstroPageMeta(), readdirSync import, backup step, and call sites wired after config promotion.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-content-debranding/07-CONTEXT.md
@.planning/phases/07-content-debranding/07-RESEARCH.md

<interfaces>
<!-- Current onboard-client.js import line (line 8) -->
From March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js:
```javascript
import { readFileSync, writeFileSync, mkdirSync, copyFileSync, existsSync } from 'node:fs';
```

<!-- CLI uses these constants already defined -->
```javascript
const WEBSITE_ROOT = join(dirname(fileURLToPath(import.meta.url)), '..');
```

<!-- Existing function pattern (follows this style) -->
```javascript
function generateBrandConfig(answers) { ... }
function generateNapData(answers) { ... }
function generateBuildPageTitle(answers) { ... }
function generateWebmanifest(answers) { ... }
```

<!-- Call site: After promotion loop completes, before build verification -->
<!-- Look for: console.log('\n\x1b[1mAll drafts promoted.\x1b[0m') -->
<!-- The build step follows shortly after with: npm run build -->

<!-- answers object shape (relevant fields) -->
```javascript
answers.legalName   // Full brand name (replaces "Impulse English Academy")
answers.shortName   // Short brand name (replaces "Impulse English")
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add replaceInMarkdownArticles and replaceInAstroPageMeta functions to onboarding CLI</name>
  <files>March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js</files>
  <read_first>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js
  </read_first>
  <action>
    Per D-09 and D-10, make 4 targeted edits to onboard-client.js:

    **Edit 1 — Update fs import (line 8):**
    Add `readdirSync` to the destructured import from 'node:fs':
    ```javascript
    import { readFileSync, writeFileSync, mkdirSync, copyFileSync, existsSync, readdirSync } from 'node:fs';
    ```

    **Edit 2 — Add replaceInMarkdownArticles function (after the last generator function, before main()):**
    Create `replaceInMarkdownArticles(answers)` that:
    1. Defines articlesDir as `join(WEBSITE_ROOT, 'src', 'content', 'articles')`
    2. Creates a timestamped backup directory: `join(WEBSITE_ROOT, 'src', 'content', 'articles-backup-' + Date.now())`
    3. Copies all .md files from articlesDir to the backup directory before any modifications (per CLAUDE.md: "Always back up files before editing")
    4. Logs the backup location: `console.log(\`  Backed up articles to ${backupDir}\`)`
    5. Reads all .md files using `readdirSync(articlesDir).filter(f => f.endsWith('.md'))`
    6. For each file:
       a. Reads content with `readFileSync(filePath, 'utf-8')`
       b. Replaces "Impulse English Academy" with `answers.legalName` FIRST (longer match first to avoid double-replacement — per research anti-pattern)
       c. Then replaces "Impulse English" with `answers.shortName`
       d. If content changed, writes back with `writeFileSync(filePath, updated, 'utf-8')`
       e. Logs each modified file with replacement count using the green checkmark pattern: `console.log(\`  \x1b[32m+\x1b[0m  ${file} (${count} replacements)\`)`
    7. Logs total replacement count at the end

    **Edit 3 — Add replaceInAstroPageMeta function (per D-10, belt-and-suspenders safety net):**
    Create `replaceInAstroPageMeta(answers)` that:
    1. Defines pagesDir as `join(WEBSITE_ROOT, 'src', 'pages')`
    2. Recursively collects all .astro files under pagesDir (use a small recursive helper with readdirSync + recursive option or manual walk)
    3. For each .astro file:
       a. Reads content with `readFileSync(filePath, 'utf-8')`
       b. Replaces hardcoded "Impulse English Academy" with `answers.legalName` (in string literals, meta props, etc.)
       c. Replaces hardcoded "Impulse English" with `answers.shortName`
       d. If content changed, writes back with `writeFileSync(filePath, updated, 'utf-8')`
       e. Logs each modified file
    4. If no files needed changes, logs: `console.log('  No residual brand references found in .astro pages (NAP imports working correctly)')`
    5. This is a safety net — Plan 07-01 converts pages to NAP imports, but this catches any files missed or added later

    **Edit 4 — Wire both call sites (after "All drafts promoted" log, before build verification):**
    Insert these lines after the promotion success message and before the build step:
    ```javascript
    // --- Article debranding -------------------------------------------------------
    console.log('\n\x1b[1mUpdating article content with new brand name...\x1b[0m');
    replaceInMarkdownArticles(answers);

    // --- Astro meta safety net (D-10) ---------------------------------------------
    console.log('\n\x1b[1mChecking .astro pages for residual brand references...\x1b[0m');
    replaceInAstroPageMeta(answers);
    ```

    **Style requirements:**
    - Match existing CLI code style: 2-space indentation, semicolons, console.log with ANSI color codes
    - Use `join()` from 'node:path' (already imported) for all file paths
    - Both functions must be idempotent — running twice should produce the same result (pure string replacement, no accumulation)
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && grep -c "replaceInMarkdownArticles" scripts/onboard-client.js | xargs test 2 -le && grep -c "replaceInAstroPageMeta" scripts/onboard-client.js | xargs test 2 -le && grep -c "readdirSync" scripts/onboard-client.js | xargs test 1 -le && grep -c "articles-backup" scripts/onboard-client.js | xargs test 1 -le && echo "PASS: both functions defined, called, with backup" || echo "FAIL: missing function, import, or backup"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "replaceInMarkdownArticles" scripts/onboard-client.js` returns at least 2 (function definition + call site)
    - `grep -c "replaceInAstroPageMeta" scripts/onboard-client.js` returns at least 2 (function definition + call site)
    - `grep -c "readdirSync" scripts/onboard-client.js` returns at least 1 (in import)
    - `grep "articles-backup" scripts/onboard-client.js` confirms backup directory creation
    - `grep "Impulse English Academy" scripts/onboard-client.js` matches inside function bodies (the string to find/replace)
    - `grep "answers.legalName" scripts/onboard-client.js` finds at least two matches (one per function)
    - `node -c scripts/onboard-client.js` exits 0 (syntax valid) — note: run from March-Impulse-Web directory
    - After running onboard with test data + build, `grep -r "Impulse" dist/` returns zero results (dist/ verification)
  </acceptance_criteria>
  <done>onboard-client.js contains both replaceInMarkdownArticles (with backup) and replaceInAstroPageMeta (D-10 safety net) functions, wired into the promotion flow before build verification. Markdown articles are backed up before modification. The script passes syntax check.</done>
</task>

</tasks>

<verification>
Run from the project root (March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/):

1. `grep "replaceInMarkdownArticles" scripts/onboard-client.js` — must show function definition AND call site
2. `grep "replaceInAstroPageMeta" scripts/onboard-client.js` — must show function definition AND call site
3. `grep "readdirSync" scripts/onboard-client.js` — must appear in import line
4. `grep "articles-backup" scripts/onboard-client.js` — must confirm backup step exists
5. `node -c scripts/onboard-client.js` — must exit 0 (no syntax errors)
6. The functions must appear AFTER the "All drafts promoted" log and BEFORE the build verification step in the source
7. After full onboarding with test data: `cd dist && grep -r "Impulse" . | wc -l` returns 0
</verification>

<success_criteria>
- replaceInMarkdownArticles function exists with backup step in onboard-client.js
- replaceInAstroPageMeta function exists in onboard-client.js (per D-10)
- readdirSync is imported from node:fs
- Markdown articles are backed up to a timestamped directory before modification
- replaceInMarkdownArticles replaces "Impulse English Academy" with answers.legalName and "Impulse English" with answers.shortName
- replaceInAstroPageMeta scans src/pages/**/*.astro for residual hardcoded brand refs and replaces them
- Longer string ("Impulse English Academy") is replaced first in both functions to prevent double-replacement
- Both functions are called after config promotion and before build verification
- Script passes Node.js syntax check
- After onboarding + build, zero "Impulse" strings appear in dist/ output
</success_criteria>

<output>
After completion, create `.planning/phases/07-content-debranding/07-02-SUMMARY.md`
</output>
