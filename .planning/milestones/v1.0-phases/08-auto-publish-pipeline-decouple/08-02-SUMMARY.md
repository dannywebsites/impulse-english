---
phase: 08-auto-publish-pipeline-decouple
plan: 02
subsystem: seo-pipeline
tags: [onboarding-cli, template-portability, auto-publish, pipeline, e2e-verification]
dependency_graph:
  requires: [brand-config.ts (with pipelineLanguage/locationCode/researchDepth), seo-writer/scripts/auto-publish.js (08-01 decoupling)]
  provides: [complete onboarding CLI generator, verified E2E import chain]
  affects: [March-Impulse-Web-.../scripts/onboard-client.js, seo-writer/scripts/auto-publish.js]
tech_stack:
  added: []
  patterns: [hardcoded pipeline defaults in generator template, brandSection naming convention]
key_files:
  created: []
  modified:
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js
    - seo-writer/scripts/auto-publish.js
decisions:
  - "Pipeline settings (pipelineLanguage, locationCode, researchDepth) use hardcoded defaults in onboard-client.js template — not interactive prompts, since they are pipeline internals not brand-voice fields"
  - "impulseSection renamed to brandSection in auto-publish.js to match Astro content schema expectation — misnamed variable would have caused build failures on generated articles"
metrics:
  duration: ~5m
  completed: 2026-04-09T18:35:00Z
  tasks_completed: 2
  files_modified: 2
---

# Phase 08 Plan 02: Onboarding CLI Integration and E2E Verification Summary

**One-liner:** Updated onboard-client.js generator to include pipeline settings fields in generated brand-config.ts, and verified the full auto-publish.js import chain resolves correctly end-to-end.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Add pipeline settings to onboard-client.js generator | 08d3c34 | March-Impulse-Web-.../scripts/onboard-client.js |
| 2 | End-to-end import verification | 3ca643b | seo-writer/scripts/auto-publish.js (deviation fix) |

## What Was Built

### Task 1: onboard-client.js pipeline settings

Inserted a `// ─── Pipeline Settings` block into the `generateBrandConfig()` function template (after `tone`, before `targetAudience`). The three fields use hardcoded defaults:

- `pipelineLanguage: 'Spanish'` — human-readable language for pipeline prompts
- `locationCode: 'Spain'` — country for DataForSEO `location_name`
- `researchDepth: 'standard'` — `standard | deep`

Comment above the block reads: `// NOTE: Edit these manually for non-Spanish or non-Spain clients`

These are NOT interactive CLI prompts — they are pipeline internals that a developer edits in brand-config.ts when setting up a non-Spanish client.

### Task 2: E2E Import Verification

All three verification checks passed:

1. **Import test:** `node --experimental-strip-types` successfully imports `BRAND_CONFIG` from `brand-config.ts` and `blogImages` from `utils/images.ts`. Output confirmed: company name, 8 image entries, and pipeline settings (`Spanish Spain standard`).

2. **S3 URL audit:** Zero `s3.us-east-1.amazonaws.com` references in `seo-writer/scripts/`.

3. **Brand reference audit:** Only filesystem path strings remain (`March-Impulse-Web-e7ad8740...` — the git directory name, unavoidable). No brand content or hardcoded brand values remain in auto-publish.js.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Renamed `impulseSection` to `brandSection` in auto-publish.js**
- **Found during:** Task 2 (brand reference audit)
- **Issue:** auto-publish.js used `impulseSection` as the local variable name and frontmatter key, but the Astro content schema (`src/content/config.ts` line 17) expects `brandSection`. The mismatch would cause Astro build failures on any article generated by the pipeline.
- **Fix:** Renamed all 4 occurrences: local variable declaration, return object shorthand, `schema.impulseSection.heading`, `schema.impulseSection.content` references, and frontmatter key in `assembleArticle()`.
- **Files modified:** seo-writer/scripts/auto-publish.js
- **Commit:** 3ca643b

## Verification Results

- `pipelineLanguage`, `locationCode`, `researchDepth` present in onboard-client.js generator template
- `node --experimental-strip-types` import test: PASS
- S3 URL audit: PASS (zero matches)
- Impulse brand content audit: PASS (only unavoidable filesystem path strings remain)
- Content schema alignment: PASS (`brandSection` now consistent between generator and Astro schema)

## Known Stubs

None — onboarding generator is complete and E2E import chain is verified.

## Self-Check: PASSED

- onboard-client.js contains `pipelineLanguage`, `locationCode`, `researchDepth`: FOUND
- auto-publish.js uses `brandSection` (not `impulseSection`): FOUND
- Commit 08d3c34 exists: FOUND
- Commit 3ca643b exists: FOUND
