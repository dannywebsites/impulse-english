---
phase: 06-component-debranding
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Logo.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Navbar.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Footer.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CoursePageLayout.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CookieBanner.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/LeadForm.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/PAAArticlePage.tsx
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/App.tsx
autonomous: true
requirements:
  - FORM-06
  - ONBD-01
must_haves:
  truths:
    - "Logo.tsx renders brand name from NAP.shortName, not hardcoded Impulse/Academy"
    - "Navbar.tsx and Footer.tsx alt text reads NAP.name, not hardcoded string"
    - "CookieBanner.tsx localStorage key is derived from NAP.shortName, not hardcoded"
    - "LeadForm.tsx GTM dataLayer uses NAP.neighborhood, not hardcoded Barrio del Pilar"
    - "PAAArticlePage.tsx visible brand text reads NAP.name"
    - "seo-system App.tsx has Toaster mounted (FORM-06 satisfied)"
  artifacts:
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Logo.tsx"
      provides: "Dynamic brand name rendering from NAP"
      contains: "NAP.shortName"
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CookieBanner.tsx"
      provides: "Brand-agnostic cookie consent key"
      contains: "NAP.shortName"
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/LeadForm.tsx"
      provides: "Dynamic GTM location from NAP"
      contains: "NAP.neighborhood"
  key_links:
    - from: "components/Logo.tsx"
      to: "utils/napData.ts"
      via: "NAP.shortName import"
      pattern: "NAP\\.shortName\\.split"
    - from: "components/CookieBanner.tsx"
      to: "utils/napData.ts"
      via: "NAP.shortName for localStorage key derivation"
      pattern: "NAP\\.shortName\\.toLowerCase"
    - from: "components/LeadForm.tsx"
      to: "utils/napData.ts"
      via: "NAP.neighborhood for GTM"
      pattern: "NAP\\.neighborhood"
---

<objective>
Replace all hardcoded Impulse brand strings in React/Astro components with NAP field imports, and verify FORM-06 (Toaster in seo-system).

Purpose: After this plan, swapping napData.ts automatically updates Logo text, alt text in navigation/footer, cookie consent key, GTM location data, and visible brand name in article pages. No component source code contains literal "Impulse" references.

Output: 7 debranded components + FORM-06 verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-component-debranding/06-CONTEXT.md
@.planning/phases/06-component-debranding/06-RESEARCH.md

<interfaces>
<!-- NAP fields available from utils/napData.ts -->
From utils/napData.ts (confirmed values):
```typescript
export const NAP = {
  name: "Impulse English Academy La Vaguada",
  shortName: "Impulse English",
  neighborhood: "La Vaguada, Barrio del Pilar",
  city: "Madrid",
  // ...other fields
};
```

Import pattern for main site components:
```typescript
import { NAP } from '../utils/napData';
```

Import pattern for seo-system components (relative path required):
```typescript
import { NAP } from '../utils/napData';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debrand Logo, Navbar, Footer, CoursePageLayout, CookieBanner, LeadForm, and PAAArticlePage visible text</name>
  <read_first>
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Logo.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Navbar.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Footer.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CoursePageLayout.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CookieBanner.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/LeadForm.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/PAAArticlePage.tsx
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/napData.ts (confirm field names)
  </read_first>
  <files>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Logo.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Navbar.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/Footer.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CoursePageLayout.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/CookieBanner.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/LeadForm.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/PAAArticlePage.tsx
  </files>
  <action>
    Apply NAP field substitution to 7 components per locked decisions D-05 through D-11.

    **Logo.tsx (per D-06):**
    - Add import: `import { NAP } from '../utils/napData';`
    - At component level (inside the component function, before return), add:
      ```
      const [brandWord, ...rest] = NAP.shortName.split(' ');
      const secondWord = rest.join(' ');
      ```
    - Line 34: Replace hardcoded `>Impulse</span>` with `>{brandWord}</span>`
    - Line 35: Replace hardcoded `>Academy</span>` with `>{secondWord}</span>`
    - If shortName has no space, secondWord will be empty string — this is expected per D-06

    **Navbar.tsx (per D-07):**
    - NAP is already imported in this file
    - Line 74 (desktop logo): Replace `alt="Impulse English Academy"` with `alt={NAP.name}`
    - Line 201 (mobile logo): Replace `alt="Impulse English Academy"` with `alt={NAP.name}`
    - Note: Check actual line numbers — they may have shifted from Phase 5 edits. Search for the alt text string.

    **Footer.tsx (per D-08):**
    - NAP is already imported in this file
    - Line 83: Replace `alt="Impulse English Academy"` with `alt={NAP.name}`
    - Note: Check actual line numbers. Search for the alt text string.

    **CoursePageLayout.tsx (per D-09):**
    - NAP is already imported in this file
    - Line 72: Replace fallback alt `"Impulse English Academy La Vaguada Madrid"` with template literal: `` `${NAP.name} ${NAP.city}` ``
    - Note: NAP.name already contains "La Vaguada" so the result is slightly different but more correct — it reads from config. If NAP.name does NOT contain a location qualifier, appending NAP.city ensures geographic context. Check the actual alt text pattern and use `${NAP.name}` if it already includes city.

    **CookieBanner.tsx (per D-05):**
    - Add import: `import { NAP } from '../utils/napData';`
    - Line 3: Replace `const CONSENT_KEY = 'impulse_cookie_consent';` with:
      ```
      const CONSENT_KEY = NAP.shortName.toLowerCase().replace(/\s+/g, '_') + '_cookie_consent';
      ```
    - Result for current NAP: `"impulse_english_cookie_consent"`

    **LeadForm.tsx (per D-11):**
    - NAP is already imported in this file
    - Find all occurrences of `'Barrio del Pilar'` (lines 49 and 82 per CONTEXT.md) and replace with `NAP.neighborhood`
    - These are in the GTM dataLayer push object — the value becomes `"La Vaguada, Barrio del Pilar"` for current NAP

    **PAAArticlePage.tsx (per D-10) — visible text only (rename handled in Plan 01):**
    - Add import: `import { NAP } from '../utils/napData';`
    - Line 126 area: Replace visible `Impulse English Academy` text with `{NAP.name}`
    - The JSX pattern is: `<span>...<BookOpen .../>Impulse English Academy</span>` — replace the literal text with `{NAP.name}`
    - Note: This file is also modified by Plan 01 for the impulseSection->brandSection rename. Since both plans are Wave 1, check if Plan 01 has already run by looking for `article.brandSection` vs `article.impulseSection`. If Plan 01 ran first, the file will already have brandSection references — that's fine, just update the visible text. If Plan 01 has NOT run, only change the visible text in this plan and leave impulseSection references for Plan 01.
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && grep -n "\"Impulse\|'Impulse\|>Impulse\|impulse_cookie_consent\|Barrio del Pilar" components/Logo.tsx components/Navbar.tsx components/Footer.tsx components/CoursePageLayout.tsx components/CookieBanner.tsx components/LeadForm.tsx components/PAAArticlePage.tsx 2>/dev/null | grep -v "import\|comment\|//" | wc -l</automated>
    Expected: 0 matches (all hardcoded Impulse strings removed)
  </verify>
  <acceptance_criteria>
    - grep "NAP.shortName.split" components/Logo.tsx returns a match
    - grep "NAP.name" components/Navbar.tsx returns at least 2 matches (desktop + mobile alt)
    - grep "NAP.name" components/Footer.tsx returns at least 1 match (alt text)
    - grep "NAP.name" components/CoursePageLayout.tsx returns at least 1 match (fallback alt)
    - grep "NAP.shortName.toLowerCase" components/CookieBanner.tsx returns a match
    - grep "NAP.neighborhood" components/LeadForm.tsx returns at least 1 match
    - grep "NAP.name" components/PAAArticlePage.tsx returns at least 1 match (visible text)
    - grep -c "impulse_cookie_consent" components/CookieBanner.tsx returns 0
    - grep "'Barrio del Pilar'" components/LeadForm.tsx returns 0 matches
  </acceptance_criteria>
  <done>All 7 main-site components use NAP field imports instead of hardcoded Impulse strings. Logo renders from NAP.shortName split, all alt text uses NAP.name, CookieBanner key derived from NAP.shortName, LeadForm GTM uses NAP.neighborhood, PAAArticlePage visible text uses NAP.name.</done>
</task>

<task type="auto">
  <name>Task 2: Verify FORM-06 (seo-system Toaster) + full build verification</name>
  <read_first>
    - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/App.tsx
  </read_first>
  <files>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/App.tsx
  </files>
  <action>
    Per D-12 and FORM-06 requirement: Verify that seo-system App.tsx already mounts `<Toaster />` from sonner.

    **Step 1 — Verify Toaster presence:**
    - Check that `import { Toaster } from 'sonner'` exists (expected at line 3)
    - Check that `<Toaster />` is rendered inside the component return JSX (expected at line 89)
    - If BOTH are present: FORM-06 is satisfied. No code change needed. Document as verified.
    - If either is MISSING: Add the import and/or `<Toaster />` inside the return JSX (before the closing fragment or div)

    **Step 2 — Full project build verification:**
    - Run `cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && PUBLIC_WEBHOOK_URL=https://test.example.com npm run build`
    - This verifies ALL changes from both Plan 01 and Plan 02 compile correctly together
    - If the build fails, diagnose whether it's a schema/frontmatter mismatch (Plan 01 issue) or a component issue (Plan 02 issue) based on the error message
    - Expected: build succeeds with zero errors

    **Step 3 — Final grep audit:**
    - Run: `grep -rn "\"Impulse\|'Impulse\|>Impulse\|impulse_cookie\|Barrio del Pilar" components/ --include="*.tsx" --include="*.ts" | grep -v "import\|//"` 
    - Expected: 0 results in component files
    - Note: seo-system mirror components (seo-system/components/) still have hardcoded strings — this is acknowledged tech debt for a future phase, not a blocker for Phase 6
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && grep -c "Toaster" seo-system/App.tsx && PUBLIC_WEBHOOK_URL=https://test.example.com npm run build 2>&1 | tail -3</automated>
    Expected: Toaster count >= 2 (import + render), build succeeds
  </verify>
  <acceptance_criteria>
    - grep "import.*Toaster.*from.*sonner" seo-system/App.tsx returns a match
    - grep "<Toaster" seo-system/App.tsx returns a match
    - npm run build exits with code 0 (with PUBLIC_WEBHOOK_URL set)
  </acceptance_criteria>
  <done>FORM-06 verified — seo-system App.tsx mounts Toaster from sonner so toast notifications are visible. Full Astro build passes with all Phase 6 changes applied. Hardcoded Impulse strings eliminated from all main-site component files.</done>
</task>

</tasks>

<verification>
1. Zero hardcoded "Impulse" strings in main-site components: `grep -rn "Impulse" March-Impulse-Web-*/components/*.tsx | grep -v import | grep -v comment | grep -v brandSection` — must return 0 results
2. FORM-06 satisfied: `grep "Toaster" March-Impulse-Web-*/seo-system/App.tsx` — shows import and render
3. Build passes: `cd March-Impulse-Web-* && PUBLIC_WEBHOOK_URL=https://test.example.com npm run build` — exits 0
</verification>

<success_criteria>
- Logo.tsx renders brand word and second word from NAP.shortName.split(' ')
- Navbar.tsx uses NAP.name for both desktop and mobile logo alt text
- Footer.tsx uses NAP.name for logo alt text
- CoursePageLayout.tsx uses NAP.name (+ NAP.city if needed) for fallback hero alt
- CookieBanner.tsx derives localStorage key from NAP.shortName
- LeadForm.tsx sends NAP.neighborhood in GTM dataLayer
- PAAArticlePage.tsx renders NAP.name as visible brand text
- seo-system App.tsx has Toaster mounted (FORM-06)
- Astro build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06-component-debranding/06-02-SUMMARY.md`
</output>
