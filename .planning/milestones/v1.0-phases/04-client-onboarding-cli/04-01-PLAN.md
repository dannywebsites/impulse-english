---
phase: 04-client-onboarding-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/package.json
autonomous: true
requirements: [ONBD-01, ONBD-02, ONBD-03, ONBD-04, ONBD-05, ONBD-06, ONBD-07, ONBD-08]

must_haves:
  truths:
    - "Running `npm run onboard` with TEMPLATE_MODE=true walks through 8 question categories"
    - "Script exits with code 1 when git remote contains 'impulse' and TEMPLATE_MODE is not set"
    - "After answering all questions, draft files appear in .draft/ directory"
    - "Colorized diff is displayed for each draft vs its original"
    - "Single Y/N confirmation promotes drafts to final locations with backups in .draft/backup/"
    - "Rejecting promotion preserves drafts without touching originals"
    - "After promotion, npm run build is executed automatically"
  artifacts:
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js"
      provides: "Complete CLI onboarding script"
      min_lines: 350
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/package.json"
      provides: "onboard script alias"
      contains: "onboard"
  key_links:
    - from: "scripts/onboard-client.js"
      to: "brand-config.ts"
      via: "generates .draft/brand-config.ts from collected answers"
      pattern: "brand-config\\.ts"
    - from: "scripts/onboard-client.js"
      to: "utils/napData.ts"
      via: "generates .draft/napData.ts from collected answers, preserves helper functions"
      pattern: "napData\\.ts"
    - from: "scripts/onboard-client.js"
      to: "utils/buildPageTitle.ts"
      via: "generates .draft/buildPageTitle.ts with new brand constants"
      pattern: "buildPageTitle\\.ts"
---

<objective>
Create the complete client onboarding CLI script that walks a developer through 8 question categories and generates fully configured draft files for brand-config.ts, napData.ts, buildPageTitle.ts, and .env.template.

Purpose: Automates TEMPLATE-SETUP.md Steps 2-4 so a developer can clone the repo, answer questions, and receive a configured website without manually editing config files.

Output: A working `scripts/onboard-client.js` that handles the full onboarding flow end-to-end.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-client-onboarding-cli/04-CONTEXT.md
@.planning/phases/04-client-onboarding-cli/04-RESEARCH.md

@brand-config.ts
@March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/napData.ts
@March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/buildPageTitle.ts
@March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/package.json

<interfaces>
<!-- Target file structures the script must generate -->

From brand-config.ts (93 lines):
```typescript
export const BRAND_CONFIG = {
  companyName: 'Impulse English Academy',
  shortName: 'Impulse English',
  legalName: 'Impulse English Academy La Vaguada',
  tagline: '...',
  industry: 'English Language Academy',
  language: 'es-ES',
  voiceRegister: 'formal-friendly',
  pronouns: 'vosotros',
  brandMentionLevel: 'subtle',
  tone: '...',
  targetAudience: '...',
  audienceSegments: ['...'],
  uniqueValue: '...',
  socialProof: '...',
  credentials: ['...'],
  primaryCta: { text: '...', href: '...' },
  secondaryCta: { text: '...', href: '...' },
  bannedWords: ['...'],
  articleRules: { minWords: 800, maxWords: 5000, minH2Sections: 3, requireFaqSection: true, faqHeading: '...', targetLength: '...' },
  impulseSectionTemplate: { headingPattern: '...', content: '...' },
  categoryTopicRef: { '...': '...' },
} as const;
export type BrandConfig = typeof BRAND_CONFIG;
```

From utils/napData.ts (154 lines):
```typescript
export const NAP = {
  name: "...", shortName: "...", legalName: "...",
  streetAddress: "...", neighborhood: "...", district: "...",
  postalCode: "...", city: "...", addressRegion: "...", country: "...",
  fullAddress: "...", shortAddress: "...",
  phone: "...", phoneRaw: "...", phoneTel: "...", whatsappUrl: "...", email: "...",
  website: "...", gbpUrl: "...",
  logo: "/images/academy/logos/img-4117.png",
  image: "/images/academy/logos/img-4117.png",
  geo: { latitude: 40.4789, longitude: -3.7114 },
  mapsEmbedUrl: "...", mapsDirectionsUrl: "...",
  openingHours: [{ dayOfWeek: "Monday", opens: "10:00", closes: "21:30" }, ...],
  openingHoursText: ["Lunes: 10:00 – 21:30", ...],
  priceRange: "...",
  social: { instagram: "...", facebook: "...", tiktok: "...", x: "...", linkedin: "...", youtube: "..." },
  sameAs: ["..."],
  areaServed: ["..."],
  metro: ["..."],
  busLines: ["..."],
  credentials: ["..."],
  aggregateRating: { ratingValue: 5, reviewCount: 150, bestRating: 5, worstRating: 1 },
  foundingDate: "2022",
} as const;

// Helper functions below NAP — must be preserved verbatim:
export function getSchemaAddress() { ... }
export function getAddressLines() { ... }
```

From utils/buildPageTitle.ts (30 lines):
```typescript
const CORE_BRAND = 'Impulse English Academy La Vaguada';
const BARRIO_SUFFIX = 'Barrio del Pilar';
const SHORT_BRAND = 'Impulse English La Vaguada';
const SEP = ' | ';
export function buildPageTitle(theme: string): string { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create CLI script with guard + question flow</name>
  <files>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/package.json
  </files>
  <read_first>
    brand-config.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/napData.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/buildPageTitle.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/package.json
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/verify-migration.mjs
    .planning/phases/04-client-onboarding-cli/04-CONTEXT.md
    .planning/phases/04-client-onboarding-cli/04-RESEARCH.md
    TEMPLATE-SETUP.md
  </read_first>
  <action>
**Step 1: Install dependencies** (per D-02 and research)

Run inside the website subdirectory:
```bash
cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97
npm install --save-dev @inquirer/prompts diff
```

**Step 2: Add "onboard" script to package.json**

Add to the `"scripts"` section of `March-Impulse-Web-.../package.json`:
```json
"onboard": "node scripts/onboard-client.js"
```

The scripts section should become:
```json
"scripts": {
  "dev": "astro dev",
  "build": "astro build",
  "preview": "astro preview",
  "onboard": "node scripts/onboard-client.js"
}
```

**Step 3: Create `scripts/onboard-client.js`** — the complete CLI script

The script must be ESM (the package.json has `"type": "module"`). Use the shebang `#!/usr/bin/env node`.

Structure the script with these sections in order:

**3a. Imports and path constants:**
```javascript
import { dirname, join } from 'node:path';
import { fileURLToPath } from 'node:url';
import { readFileSync, writeFileSync, mkdirSync, copyFileSync, existsSync } from 'node:fs';
import { execSync } from 'node:child_process';
import { input, confirm, select, editor } from '@inquirer/prompts';
import { createPatch } from 'diff';

const __dirname = dirname(fileURLToPath(import.meta.url));
const WEBSITE_ROOT = join(__dirname, '..');
const PROJECT_ROOT = join(__dirname, '../..');
const DRAFT_DIR = join(PROJECT_ROOT, '.draft');
const BACKUP_DIR = join(DRAFT_DIR, 'backup');
```

**3b. TEMPLATE_MODE guard** (per D-10, D-11, D-12 — MUST be first executable logic):

Function `isProductionRepo()`:
- If `process.env.TEMPLATE_MODE === 'true'`, return false (allow override).
- Otherwise, run `execSync('git remote -v', { cwd: PROJECT_ROOT, encoding: 'utf-8' })`.
- If output contains "impulse" (case-insensitive), return true.
- On error (not a git repo), return false.

If `isProductionRepo()` returns true:
- Print to stderr: `'ERROR: This appears to be the production Impulse repo.\nSet TEMPLATE_MODE=true to override, or run on a fresh clone.'`
- `process.exit(1)`

**3c. Parse `--skip-build` flag** (discretionary per CONTEXT.md):
```javascript
const skipBuild = process.argv.includes('--skip-build');
```

**3d. Main async function with question flow** (per D-01, D-03, D-04):

Create `async function main()` containing the full question flow organized in 8 sections. Each section prints a header like:
```javascript
console.log('\n─── (1) Business Identity ─────────────────────────────\n');
```

Every question uses the current Impulse value as the `default` (per D-03). Collect all answers into a single `answers` object.

**Category 1: Business Identity** — collect:
- `companyName` (default: `'Impulse English Academy'`)
- `shortName` (default: `'Impulse English'`)
- `legalName` (default: `'Impulse English Academy La Vaguada'`)
- `tagline` (default: `'Centro Preparador Oficial Cambridge en La Vaguada, Madrid. 100% de aprobados en 2024-2025.'`)
- `industry` (default: `'English Language Academy'`)
- `foundingDate` (default: `'2022'`)

**Category 2: Contact Info** — collect:
- `phone` (default: `'+34 604 910 611'`) — formatted with country code
- `email` (default: `'info@impulse-english.es'`)
- `website` (default: `'https://impulse-english.es'`)
- `gbpUrl` (default: `'https://share.google/GuRfJ3TjrnIIUhrdk'`)

Derive (no prompt): `phoneRaw` (strip spaces/dashes from phone), `phoneTel` (`'tel:' + phoneRaw`), `whatsappUrl` (`'https://wa.me/' + phoneRaw.replace('+', '')`)

**Category 3: Geo & Address** — collect:
- `streetAddress` (default: `'Av. de El Ferrol, 22'`)
- `neighborhood` (default: `'La Vaguada, Barrio del Pilar'`)
- `district` (default: `'Fuencarral-El Pardo'`)
- `postalCode` (default: `'28029'`)
- `city` (default: `'Madrid'`)
- `addressRegion` (default: `'Community of Madrid'`)
- `country` (default: `'ES'`)
- `latitude` as string input, parsed to float (default: `'40.4789'`)
- `longitude` as string input, parsed to float (default: `'-3.7114'`)
- `mapsEmbedUrl` (default: current Impulse value) — add note: "Get from Google Maps > Share > Embed a map"
- `priceRange` (default: `'€64 - €79/mes'`)
- `areaServed` using `editor` prompt, pre-populated with current areas one per line (default: join Impulse areas with newlines). Split result by newlines, filter empty, trim.
- `metro` using `editor` prompt, pre-populated with current metro lines one per line. Same split logic. Default to empty array if blank.
- `busLines` using `editor` prompt, pre-populated with current bus lines one per line. Same split logic. Default to empty array if blank.

Derive: `fullAddress` (`streetAddress + ', ' + neighborhood + ', ' + postalCode + ' ' + city`), `shortAddress` (`streetAddress + ', ' + postalCode + ' ' + city`), `mapsDirectionsUrl` (`'https://www.google.com/maps/dir/?api=1&destination=' + encodeURIComponent(streetAddress + ' ' + postalCode + ' ' + city + ' ' + country)`)

**Category 4: Opening Hours** (per D-04):
- Loop through 7 days: Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday
- For each day: ask `confirm` "Is {day} open?" with default true for Mon-Fri, false for Sat-Sun
- If open: ask `input` for open time (default varies by Impulse values: Mon/Wed '10:00', Tue/Thu '15:30', Fri '13:30') and close time (default: Mon-Thu '21:30', Fri '19:30')
- Build `openingHours` array (only open days, with `{ dayOfWeek, opens, closes }`)
- Build `openingHoursText` array with human-readable format. Use Spanish day names mapping: Monday=Lunes, Tuesday=Martes, Wednesday=Miercoles (with accent: Miércoles), Thursday=Jueves, Friday=Viernes, Saturday=Sabado (Sábado), Sunday=Domingo. For open days: `"Lunes: 10:00 – 21:30"`. Group consecutive closed days: if both Sat and Sun are closed, use `"Sábado – Domingo: Cerrado"` as a single entry.

**Category 5: Social Profiles** — collect:
- `instagram` (default: `'https://www.instagram.com/impulse_english_lavaguada/'`)
- `facebook` (default: `'https://www.facebook.com/impulseenglishlavaguada/'`)
- `tiktok` (default: `'https://www.tiktok.com/@impulse_english_lavaguada'`)
- `x` (default: `'https://x.com/impulse_vaguada'`)
- `linkedin` (default: `'https://www.linkedin.com/company/101859096/'`)
- `youtube` (default: `'https://www.youtube.com/@Impulse-English'`)

Each prompt should say: "Leave empty to skip"

Derive: `sameAs` array — filter non-empty values from `[gbpUrl, instagram, facebook, tiktok, x, linkedin, youtube]`

**Category 6: Brand Voice & Language** — collect:
- `language` (default: `'es-ES'`) — note: BCP 47 tag
- `voiceRegister` using `select` with choices: `formal-friendly`, `casual`, `professional`, `academic` (default: `'formal-friendly'`)
- `pronouns` using `select` with choices: `vosotros (Spain)`, `ustedes (LATAM)`, `you (English)` — values: `'vosotros'`, `'ustedes'`, `'you'` (default: `'vosotros'`)
- `brandMentionLevel` using `select` with choices: `subtle`, `moderate`, `prominent` (default: `'subtle'`)
- `tone` (default: `'Professional yet approachable. Authority through results, not academic jargon.'`)
- `targetAudience` (default: Impulse value)
- `uniqueValue` (default: Impulse value)
- `socialProof` (default: Impulse value)
- `audienceSegments` using `editor` prompt, pre-populated with Impulse segments one per line. Split by newlines, filter empty, trim.

**Category 7: Credentials & Ratings** — collect:
- `credentials` using `editor` prompt, pre-populated with Impulse credentials one per line
- `ratingValue` as number string parsed to float (default: `'5'`)
- `reviewCount` as number string parsed to int (default: `'150'`)

Derive: `bestRating: 5`, `worstRating: 1` (fixed schema.org convention)

**Category 8: CTA Defaults** — collect:
- `primaryCtaText` (default: `'Prueba de nivel gratis'`)
- `primaryCtaHref` (default: `'/reservar-clase'`)
- `secondaryCtaText` (default: `'Nuestros cursos'`)
- `secondaryCtaHref` (default: `'/cursos-ingles/adultos'`)
- `CORE_BRAND` for buildPageTitle.ts (default: `'Impulse English Academy La Vaguada'`)
- `BARRIO_SUFFIX` (default: `'Barrio del Pilar'`)
- `SHORT_BRAND` (default: `'Impulse English La Vaguada'`)

**3e. File generation functions:**

Create 4 generator functions that return file content as strings:

`generateBrandConfig(answers)` — returns the complete brand-config.ts content using template literals. CRITICAL: use single-quoted strings for all string values in the generated TypeScript (not backticks) to avoid template literal escaping issues. Escape any single quotes in user input values with `value.replace(/'/g, "\\'")`. The file must end with `} as const;\n\nexport type BrandConfig = typeof BRAND_CONFIG;\n`. Include the `bannedWords` array verbatim from the original (not client-specific). Include `articleRules` verbatim. For `impulseSectionTemplate`, use `{companyName}`, `{tagline}`, `{socialProof}` as literal placeholder strings (these are template patterns, not interpolated). For `categoryTopicRef`, use the `editor` prompt (pre-populated with Impulse mappings in `key: value` format one per line) collected during Category 6 or 8, OR use the Impulse defaults and document in post-generation output that this needs manual editing for non-English-academy clients.

`generateNapData(answers)` — returns the complete napData.ts content. CRITICAL: read the original `napData.ts` file to extract the helper functions (lines 138-154: `getSchemaAddress()` and `getAddressLines()`). Append these verbatim after the NAP const. The `logo` and `image` fields should keep the value `'/images/academy/logos/img-4117.png'` with a comment `// REQUIRED: Replace with your logo path`. Arrays (openingHours, openingHoursText, areaServed, metro, busLines, credentials, sameAs) must be generated as properly formatted TypeScript arrays.

`generateBuildPageTitle(answers)` — returns the complete buildPageTitle.ts. Only the 3 constants at the top change (CORE_BRAND, BARRIO_SUFFIX, SHORT_BRAND). Read the original file and replace lines 1-3 with the new values, keeping the rest (SEP constant and function body) verbatim.

`generateEnvTemplate()` — returns:
```
# Environment variables for [companyName] website
# Copy this file to .env and fill in your values

# Webhook URL for lead form submissions (required)
# This is the endpoint that receives form data when a visitor submits the contact form
PUBLIC_WEBHOOK_URL=https://your-webhook-endpoint.com/submit
```

**3f. Draft writing:**

Create `.draft/` directory with `mkdirSync(DRAFT_DIR, { recursive: true })`.

Write 4 files:
- `join(DRAFT_DIR, 'brand-config.ts')` — from `generateBrandConfig(answers)`
- `join(DRAFT_DIR, 'napData.ts')` — from `generateNapData(answers)`
- `join(DRAFT_DIR, 'buildPageTitle.ts')` — from `generateBuildPageTitle(answers)`
- `join(DRAFT_DIR, '.env.template')` — from `generateEnvTemplate()`

Per D-05.

**3g. Colorized diff display** (per D-06):

For each of the 4 draft files, read the original file and display a colorized unified diff using the `diff` package's `createPatch`. Use ANSI escape codes: green (`\x1b[32m`) for additions, red (`\x1b[31m`) for removals, reset (`\x1b[0m`). Print a header before each diff: `'\n=== {filename} ===\n'`.

For `.env.template`, if no original exists, show the entire draft as additions.

**3h. Promotion flow** (per D-07, D-08, D-09):

Single `confirm` prompt: `'Promote these drafts to final files?'` (default: false for safety).

On rejection (per D-08): print `'\nDrafts preserved in .draft/ directory. You can edit them manually and re-run the script.'`. Exit cleanly.

On acceptance (per D-09):
1. Create `.draft/backup/` directory
2. Back up originals to `.draft/backup/`:
   - `brand-config.ts` -> `.draft/backup/brand-config.ts.bak`
   - `utils/napData.ts` -> `.draft/backup/napData.ts.bak`
   - `utils/buildPageTitle.ts` -> `.draft/backup/buildPageTitle.ts.bak`
3. Copy drafts to final locations:
   - `.draft/brand-config.ts` -> `PROJECT_ROOT/brand-config.ts`
   - `.draft/napData.ts` -> `WEBSITE_ROOT/utils/napData.ts`
   - `.draft/buildPageTitle.ts` -> `WEBSITE_ROOT/utils/buildPageTitle.ts`
   - `.draft/.env.template` -> `WEBSITE_ROOT/.env.template`

**3i. Build verification** (per D-13, D-14, D-15):

If `--skip-build` flag is NOT set:
- Print `'\nRunning npm run build to verify configuration...\n'`
- Run `execSync('npm run build', { cwd: WEBSITE_ROOT, stdio: 'inherit' })` in a try/catch
- On success (per D-15): print success message with next steps listing TEMPLATE-SETUP.md Steps 5-10:
  ```
  Build successful!

  Next steps (see TEMPLATE-SETUP.md):
  5. Install skills & agents (copy .claude/ directory)
  6. Update brand-voice-enforcer with new brand voice
  7. Update auto-publish-pipeline categories
  8. Update vercel.json redirects for new domain
  9. Update CLAUDE.md project description
  10. Update Google Analytics / GTM IDs
  ```
- On failure (per D-14): print `'\nBuild failed. Review the errors above and fix the generated config files.'`. Do NOT rollback.

If `--skip-build` IS set: print `'\nSkipping build verification (--skip-build). Run npm run build manually to verify.'`

**3j. Call `main()` at the bottom:**
```javascript
main().catch((err) => {
  if (err.name === 'ExitPromptError') {
    console.log('\nOnboarding cancelled.');
    process.exit(0);
  }
  console.error(err);
  process.exit(1);
});
```

The `ExitPromptError` catch handles Ctrl+C during prompts (standard @inquirer/prompts behavior).
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && node -e "import('./scripts/onboard-client.js').catch(e => { if (e.code === 'ERR_MODULE_NOT_FOUND') { console.error('Missing import:', e.message); process.exit(1); } })" && echo "Module loads OK"</automated>
  </verify>
  <acceptance_criteria>
    - scripts/onboard-client.js exists and starts with `#!/usr/bin/env node`
    - scripts/onboard-client.js contains `import { input, confirm, select, editor } from '@inquirer/prompts'`
    - scripts/onboard-client.js contains `import { createPatch } from 'diff'`
    - scripts/onboard-client.js contains `isProductionRepo` function with `impulse` string check
    - scripts/onboard-client.js contains `process.exit(1)` in the guard block
    - scripts/onboard-client.js contains `TEMPLATE_MODE` string
    - scripts/onboard-client.js contains all 8 section headers: `(1) Business Identity`, `(2) Contact Info`, `(3) Geo`, `(4) Opening Hours`, `(5) Social`, `(6) Brand Voice`, `(7) Credentials`, `(8) CTA`
    - scripts/onboard-client.js contains `generateBrandConfig` function
    - scripts/onboard-client.js contains `generateNapData` function
    - scripts/onboard-client.js contains `generateBuildPageTitle` function
    - scripts/onboard-client.js contains `generateEnvTemplate` function
    - scripts/onboard-client.js contains `createPatch` call for diff display
    - scripts/onboard-client.js contains `.draft/` path references
    - scripts/onboard-client.js contains `.draft/backup/` path reference
    - scripts/onboard-client.js contains `npm run build` execution via execSync
    - scripts/onboard-client.js contains `--skip-build` flag handling
    - scripts/onboard-client.js contains `as const` in generated TypeScript output
    - scripts/onboard-client.js contains `getSchemaAddress` and `getAddressLines` preservation logic
    - scripts/onboard-client.js contains `ExitPromptError` catch
    - package.json contains `"onboard": "node scripts/onboard-client.js"`
    - `@inquirer/prompts` appears in package.json devDependencies
    - `diff` appears in package.json devDependencies
    - `node -e "import('./scripts/onboard-client.js')"` in the website dir does not throw ERR_MODULE_NOT_FOUND
  </acceptance_criteria>
  <done>
    The complete onboard-client.js script exists with all 8 question categories, 4 file generators, TEMPLATE_MODE guard, draft/diff/promotion flow, build verification, and --skip-build flag. Dependencies installed. Package.json has the onboard script alias.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add .draft/ to .gitignore and run smoke test</name>
  <files>
    .gitignore
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/.gitignore
  </files>
  <read_first>
    .gitignore
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/.gitignore
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/onboard-client.js
  </read_first>
  <action>
**Step 1: Add `.draft/` to root .gitignore**

The root `.gitignore` currently contains only `*.zip`. Append:
```
.draft/
```

**Step 2: Add `.draft/` to website .gitignore**

Append `.draft/` to the website's `.gitignore` under a new section:
```
# Onboarding drafts
.draft/
```

**Step 3: Smoke test the TEMPLATE_MODE guard**

Run the script without TEMPLATE_MODE set. It should exit with code 1 because the git remote contains "impulse":
```bash
cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97
node scripts/onboard-client.js 2>&1 || true
```

Verify the output contains "production Impulse repo" and the exit code was 1.

**Step 4: Verify the script loads with TEMPLATE_MODE=true**

Run with TEMPLATE_MODE=true but immediately Ctrl+C (or use a timeout). The script should get past the guard and start showing the first question category header:
```bash
cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97
timeout 3 env TEMPLATE_MODE=true node scripts/onboard-client.js 2>&1 || true
```

Verify the output contains "(1) Business Identity" section header.
  </action>
  <verify>
    <automated>cd "/Users/danny/Desktop/templated website 2026/March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97" && node scripts/onboard-client.js 2>&1; test $? -eq 1 && echo "Guard test PASSED" || echo "Guard test FAILED"</automated>
  </verify>
  <acceptance_criteria>
    - Root .gitignore contains `.draft/`
    - Website .gitignore contains `.draft/`
    - Running `node scripts/onboard-client.js` without TEMPLATE_MODE exits with code 1
    - Exit message contains "production Impulse repo"
  </acceptance_criteria>
  <done>
    The .draft/ directory is excluded from git tracking. The TEMPLATE_MODE guard correctly prevents execution on the production repo (exits with code 1 and clear error message).
  </done>
</task>

</tasks>

<verification>
1. `scripts/onboard-client.js` exists in the website's scripts directory with 350+ lines
2. `package.json` has `"onboard"` script alias
3. `@inquirer/prompts` and `diff` are in devDependencies
4. `.gitignore` (both root and website) contain `.draft/`
5. Running `node scripts/onboard-client.js` without TEMPLATE_MODE exits with code 1
6. The script imports load without MODULE_NOT_FOUND errors
</verification>

<success_criteria>
- The complete onboarding CLI script exists with all 8 question categories, 4 file generators, guard, draft/diff/promotion UX, and build verification
- Dependencies are installed and the script's module imports resolve
- The TEMPLATE_MODE guard correctly blocks execution on the production repo
- `.draft/` is gitignored at both root and website levels
</success_criteria>

<output>
After completion, create `.planning/phases/04-client-onboarding-cli/04-01-SUMMARY.md`
</output>
