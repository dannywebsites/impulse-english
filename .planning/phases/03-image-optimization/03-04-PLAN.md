---
phase: 03-image-optimization
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified:
  - scripts/optimize-images.mjs (DELETE)
  - seo-system/scripts/optimize-images.mjs (DELETE)
  - components/OptimizedImage.tsx (DELETE)
  - public/images/optimized/ (DELETE directory)
  - public/images/original/ (DELETE directory)
  - utils/images.ts
autonomous: false
requirements: [OPT-04]
must_haves:
  truths:
    - "scripts/optimize-images.mjs does not exist"
    - "seo-system/scripts/optimize-images.mjs does not exist"
    - "components/OptimizedImage.tsx does not exist"
    - "public/images/optimized/ directory does not exist"
    - "public/images/original/ directory does not exist"
    - "OptimizedImageData and ImageSize types are removed from utils/images.ts"
    - "facilityImages, infantilImages, studentImages, certificationImages, brandingImages, teamImages, courseImages exports (OptimizedImageData shape) are removed or converted to simple URL strings in utils/images.ts"
    - "npm run build completes without errors"
    - "No source file references /images/optimized/ paths"
  artifacts:
    - path: "utils/images.ts"
      provides: "Cleaned registry with only S3ImageData and AcademyImage types (no OptimizedImageData)"
  key_links:
    - from: "utils/images.ts"
      to: "seo-system consumers"
      via: "S3ImageData url strings pointing to /images/academy/"
      pattern: "url: '/images/academy/"
---

<objective>
Delete the entire hand-rolled image optimization system and clean up utils/images.ts by removing OptimizedImageData types and exports that are no longer consumed.

Purpose: Completes OPT-04 — the hand-rolled system is fully replaced by Astro's native pipeline. Removes 211+ generated files, 2 scripts, 1 component, and legacy type definitions.
Output: Clean codebase with no hand-rolled optimization artifacts. Build passes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-image-optimization/03-CONTEXT.md
@.planning/phases/03-image-optimization/03-RESEARCH.md
@.planning/phases/03-image-optimization/03-02-SUMMARY.md
@.planning/phases/03-image-optimization/03-03-SUMMARY.md
@March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/images.ts
@March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/images-astro.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete hand-rolled artifacts and clean up images.ts types</name>
  <files>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/optimize-images.mjs (DELETE)
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/scripts/optimize-images.mjs (DELETE)
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/OptimizedImage.tsx (DELETE)
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/public/images/optimized/ (DELETE directory)
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/public/images/original/ (DELETE directory)
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/images.ts
  </files>
  <read_first>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/images.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/components/OptimizedImage.tsx
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/scripts/optimize-images.mjs
  </read_first>
  <action>
    **Per D-03, D-10, D-11, D-12, D-14:**

    Step 1: Verify no remaining consumers of files to be deleted:
    ```bash
    grep -rn "OptimizedImage" components/ pages/ src/ --include="*.tsx" --include="*.ts" --include="*.astro"
    grep -rn "optimize-images" scripts/ seo-system/scripts/ package.json
    grep -rn "/images/optimized/" components/ pages/ src/ --include="*.tsx" --include="*.ts" --include="*.astro"
    ```
    If ANY results appear, those files still have references that need updating. Do NOT proceed with deletion until references are zero. Fix any remaining references first.

    Step 2: Delete files (per D-10, D-11, D-12):
    ```bash
    rm scripts/optimize-images.mjs
    rm seo-system/scripts/optimize-images.mjs
    rm components/OptimizedImage.tsx
    ```

    Step 3: Delete directories (per D-03):
    ```bash
    rm -rf public/images/optimized/     # 211 hand-generated files
    rm -rf public/images/original/       # 23 source originals (now in src/assets/images/academy/)
    ```

    Step 4: Clean up utils/images.ts (per D-14):

    Remove these type definitions:
    - `export interface ImageSize { webp: string; jpg: string; width: number; }`
    - `export interface OptimizedImageData { name: string; alt: string; altEn: string; category: string; sizes: { sm: ImageSize; md: ImageSize; lg: ImageSize; xl: ImageSize; }; }`
    - `export interface ImageEntry` (if no longer used)
    - `export interface ImageSet` (if no longer used)

    Remove or convert these exports that use OptimizedImageData shape (have `sizes` with sm/md/lg/xl):
    - `facilityImages` (lines ~72-137) — DELETE entire export. The s3FacilityImages export (S3ImageData shape, /images/academy/ URLs) replaces its functionality.
    - `infantilImages` (lines ~185-229) — DELETE. The s3InfantilImages export replaces it.
    - `studentImages` (lines ~230-261) — DELETE. Images available via s3ReviewImages or other S3 exports.
    - `certificationImages` (lines ~262-332) — DELETE. Images available via s3CambridgeImages.
    - `brandingImages` (lines ~333-372) — DELETE.
    - `teamImages` (lines ~373-391) — CONVERT to S3ImageData shape with `/images/academy/team/` URL string instead of sizes object. Keep this export because TeamSection still references teamImages for non-hero images (like the team member photos). Only keep what's still consumed — check grep results.
    - `courseImages` (lines ~1143-1192) — DELETE. Astro pipeline handles course images now via getImage() in .astro parents.

    Keep these exports UNCHANGED:
    - `S3ImageData` interface
    - `AcademyImage` interface
    - All `s3*` exports (s3FacilityImages, s3ReviewImages, s3SecondaryImages, s3InfantilImages, s3LinguaskillImages, s3CambridgeImages)
    - All `*GalleryImages` arrays (infantilGalleryImages, primariaGalleryImages, etc.)
    - `allImages` and `allS3Images` aggregation objects — update to remove deleted entries
    - `blogImages` export

    Step 5: Update `allImages` and `allS3Images` to remove references to deleted exports.

    Step 6: Check if any package.json scripts reference optimize-images:
    ```bash
    grep "optimize" package.json seo-system/package.json
    ```
    Remove any npm scripts that invoke optimize-images.mjs.

    Step 7: Final audit — search entire codebase for any remaining /images/optimized/ references:
    ```bash
    grep -rn "/images/optimized" . --include="*.tsx" --include="*.ts" --include="*.astro" --include="*.mjs" --include="*.js" | grep -v node_modules | grep -v .planning
    ```
    This should return zero results (except possibly markdown documentation files which are acceptable).
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && test ! -f scripts/optimize-images.mjs && test ! -f seo-system/scripts/optimize-images.mjs && test ! -f components/OptimizedImage.tsx && test ! -d public/images/optimized && test ! -d public/images/original && grep -c "OptimizedImageData" utils/images.ts; npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - scripts/optimize-images.mjs does NOT exist
    - seo-system/scripts/optimize-images.mjs does NOT exist
    - components/OptimizedImage.tsx does NOT exist
    - public/images/optimized/ directory does NOT exist
    - public/images/original/ directory does NOT exist
    - `grep -c "OptimizedImageData" utils/images.ts` returns 0
    - `grep -c "ImageSize" utils/images.ts` returns 0
    - `grep -rn "/images/optimized" . --include="*.tsx" --include="*.ts" --include="*.astro" | grep -v node_modules | grep -v .planning` returns 0 results
    - utils/images.ts still exports s3FacilityImages, s3ReviewImages, s3CambridgeImages, s3LinguaskillImages, blogImages, all gallery image arrays
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>Hand-rolled optimization system fully removed. No scripts, no OptimizedImage component, no /images/optimized/ directory, no OptimizedImageData types.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of image quality across key pages</name>
  <files>None — verification only</files>
  <action>
    Start the dev server and verify all images render correctly across key pages. This is a visual check that cannot be automated — confirming image quality, aspect ratios, and responsive behavior requires human eyes.

    1. Run `cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && npm run dev`
    2. Open each URL listed in how-to-verify and confirm images load
    3. Check DevTools Network tab for avif/webp format delivery on homepage
  </action>
  <read_first>None — this is visual verification of running application</read_first>
  <what-built>
    The entire image pipeline has been migrated from a hand-rolled Sharp script + OptimizedImage.tsx + 211 pre-generated files to Astro's native Image/Picture/getImage() pipeline. All hero images now serve avif/webp via Astro at build time. All React components receive optimized URLs via props.
  </what-built>
  <how-to-verify>
    1. Run `cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && npm run dev`
    2. Open http://localhost:4321/ — verify homepage hero image loads correctly (not broken), check that it loads fast (eager)
    3. Open http://localhost:4321/cursos-ingles/infantil/ — verify hero image loads
    4. Open http://localhost:4321/examenes-cambridge/ — verify hero image loads
    5. Open http://localhost:4321/sobre-nosotros/ — verify hero image loads
    6. Open http://localhost:4321/contacto/ — verify hero image loads
    7. Check responsive behavior: resize browser to mobile width (less than 640px) — images should still display correctly
    8. Open browser DevTools Network tab, reload homepage — verify hero image is served as avif or webp format (check Content-Type or filename in network panel)
    9. Run `npm run build` — verify build completes with no errors and note output size
  </how-to-verify>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && npm run build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - All listed pages display hero images without broken image icons
    - Homepage hero loads in avif or webp format (visible in DevTools Network tab)
    - Images display correctly at mobile viewport widths
    - `npm run build` completes without errors
  </acceptance_criteria>
  <done>User has visually confirmed all images render correctly across key pages after full pipeline migration</done>
  <resume-signal>Type "approved" if all pages display images correctly, or describe any broken/missing images</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` completes without errors
- Zero files reference /images/optimized/ in source code
- No hand-rolled optimization scripts or components exist
- All images load correctly on key pages
</verification>

<success_criteria>
Hand-rolled image optimization system completely removed. Astro native pipeline serves all images. Build passes. Visual verification confirms no broken images.
</success_criteria>

<output>
After completion, create `.planning/phases/03-image-optimization/03-04-SUMMARY.md`
</output>
