---
phase: 02-image-consolidation
plan: 05
type: execute
wave: 3
depends_on: ["02-02", "02-03", "02-04"]
files_modified:
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/src/data/images.ts
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/src/data/academyImages.ts
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/src/data/images.ts
  - March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/src/data/academyImages.ts
autonomous: false
requirements:
  - IMG-01
  - IMG-02
  - IMG-03
  - IMG-04

must_haves:
  truths:
    - "Zero S3 URLs exist anywhere in the codebase (.ts, .tsx, .astro files)"
    - "Old data files (src/data/images.ts, src/data/academyImages.ts) are deleted from both main and seo-system"
    - "The Astro build completes successfully with zero errors"
    - "All image imports resolve through utils/images.ts"
  artifacts:
    - path: "March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/utils/images.ts"
      provides: "Single source of truth for all image references"
  key_links:
    - from: "All components and pages"
      to: "utils/images.ts"
      via: "import statements"
      pattern: "utils/images"
---

<objective>
Delete old image data files, run full codebase verification, and confirm build succeeds.

Purpose: Final cleanup and verification that the entire image consolidation is complete. Deletes the now-unused data files (per D-03, D-08) and runs the definitive S3 URL audit plus Astro build.
Output: Clean codebase with zero S3 URLs, no orphaned data files, passing build.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-image-consolidation/02-CONTEXT.md
@.planning/phases/02-image-consolidation/02-01-SUMMARY.md
@.planning/phases/02-image-consolidation/02-02-SUMMARY.md
@.planning/phases/02-image-consolidation/02-03-SUMMARY.md
@.planning/phases/02-image-consolidation/02-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete old data files and run full S3 URL audit</name>
  <files>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/src/data/images.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/src/data/academyImages.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/src/data/images.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system/src/data/academyImages.ts
  </files>
  <read_first>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/src/data/images.ts
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/src/data/academyImages.ts
  </read_first>
  <action>
    **Step 1: Pre-deletion verification.**
    Run a comprehensive grep to ensure NO file still imports from the old data files:
    ```bash
    grep -r "from.*src/data/images\|from.*src/data/academyImages" \
      March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/ \
      --include="*.ts" --include="*.tsx" --include="*.astro" \
      | grep -v node_modules
    ```
    If ANY results appear, those files still depend on the old data files. Fix them before deletion by updating their imports to point to `utils/images.ts` (main site: `@/utils/images`, seo-system: relative path `../../utils/images`).

    **Step 2: Delete old data files** (per D-03 and D-08):
    - Delete `src/data/images.ts` (main site old registry)
    - Delete `src/data/academyImages.ts` (main site academy images)
    - Delete `seo-system/src/data/images.ts` (seo-system mirror)
    - Delete `seo-system/src/data/academyImages.ts` (seo-system mirror)

    **Step 3: Full S3 URL audit.**
    Run the definitive S3 URL search across the ENTIRE codebase:
    ```bash
    grep -r "impulseenglish.s3\|impulse-english.es/images" \
      March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/ \
      --include="*.ts" --include="*.tsx" --include="*.astro" --include="*.md" \
      | grep -v node_modules | grep -v scripts/download-s3-images
    ```
    Expected: ZERO results (the download script itself may contain S3 URLs — that's fine, exclude it).

    If any S3 URLs remain, fix them by replacing with registry imports or local paths.

    **Step 4: Check for any orphaned imports** — files that import from deleted paths will now cause TypeScript errors. This is caught by the build in Task 2.
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && grep -r "impulseenglish.s3\|impulse-english.es/images" --include="*.ts" --include="*.tsx" --include="*.astro" | grep -v node_modules | grep -v scripts/download-s3 | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/data/images.ts 2>/dev/null` returns "No such file" (deleted)
    - `ls src/data/academyImages.ts 2>/dev/null` returns "No such file" (deleted)
    - `ls seo-system/src/data/images.ts 2>/dev/null` returns "No such file" (deleted)
    - `ls seo-system/src/data/academyImages.ts 2>/dev/null` returns "No such file" (deleted)
    - Full S3 URL grep (excluding scripts/ and node_modules) returns 0 matches
    - `grep -r "from.*src/data/images\|from.*src/data/academyImages" --include="*.ts" --include="*.tsx" | grep -v node_modules | wc -l` returns 0
  </acceptance_criteria>
  <done>Old data files deleted. Zero S3 URLs in codebase. No orphaned imports to deleted files.</done>
</task>

<task type="auto">
  <name>Task 2: Run Astro build to verify everything compiles</name>
  <files></files>
  <read_first>
    March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/package.json
  </read_first>
  <action>
    Run the full Astro production build to verify that:
    1. All import paths resolve correctly (no broken imports to deleted files)
    2. TypeScript compilation succeeds (no type errors from registry changes)
    3. All pages render without errors (no missing image references)

    ```bash
    cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && npm run build
    ```

    If the build fails:
    - Read the error output to identify which file and import is broken
    - Fix the import path (likely a missed migration from Plans 02-04)
    - Re-run the build until it passes

    Also verify the seo-system builds if it has its own build command:
    ```bash
    cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97/seo-system && npm run build 2>/dev/null || echo "No seo-system build command"
    ```
  </action>
  <verify>
    <automated>cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build` exits with code 0
    - Build output shows pages generated successfully (e.g., "generating static routes" or "build complete")
    - No TypeScript errors in build output
    - No "Cannot find module" errors referencing src/data/images or src/data/academyImages
  </acceptance_criteria>
  <done>Astro production build completes with zero errors. All imports resolve. All pages generate successfully.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of image loading across all page types</name>
  <files></files>
  <action>
    Start the dev server and visually verify that images load correctly across all page types.
    What was built: Complete image consolidation — 73 S3 images downloaded locally, central registry created at utils/images.ts, all 153 files migrated to use local references, old data files deleted, build passing.
    How to verify:
    1. Run `cd March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97 && npm run dev`
    2. Open http://localhost:4321/ — verify homepage images load (logo, hero, team photos)
    3. Navigate to a course page (e.g., /cursos-ingles/adultos/) — verify gallery images load
    4. Navigate to a location page (e.g., /academia-ingles-la-vaguada/) — verify location photos load
    5. Navigate to any blog page — verify hero image loads
    6. Check the footer — verify partner logos display
    7. Right-click any image and open in new tab — URL should be localhost:4321/images/... (local), NOT impulseenglish.s3...
    Resume signal: Type "approved" if images load correctly across all page types, or describe any missing/broken images.
  </action>
  <verify>User confirms images load correctly across homepage, course, location, blog, and footer</verify>
  <done>User has visually verified that all images load from local paths across all page types.</done>
</task>

</tasks>

<verification>
- S3 URL audit: `grep -r "impulseenglish.s3\|impulse-english.es/images" --include="*.ts" --include="*.tsx" --include="*.astro" | grep -v node_modules | grep -v scripts/download-s3 | wc -l` returns 0
- Build: `npm run build` exits 0
- Old files deleted: `ls src/data/images.ts src/data/academyImages.ts seo-system/src/data/images.ts seo-system/src/data/academyImages.ts 2>&1 | grep -c "No such file"` returns 4
- Visual: User confirms images load on dev server
</verification>

<success_criteria>
All four requirements verified:
- IMG-01: All S3 images downloaded to public/images/academy/ (73 files)
- IMG-02: Central registry at utils/images.ts is the single source of truth
- IMG-03: All component files import through the central registry
- IMG-04: seo-system components use the shared registry via relative paths
Zero S3 URLs remain. Build passes. User confirms visual correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/02-image-consolidation/02-05-SUMMARY.md`
</output>
