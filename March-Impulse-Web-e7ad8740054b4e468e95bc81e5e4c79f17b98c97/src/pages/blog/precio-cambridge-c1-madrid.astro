---
import BaseLayout from '../../layouts/BaseLayout.astro';
import PrecioCambridgeC1MadridPage, { articleSchema, faqs } from '../../../pages/blog/PrecioCambridgeC1MadridPage';
import CookieBanner from '../../../components/CookieBanner';
import { generateFAQSchema } from '../../../utils/schemaData';

const schemas = [
  articleSchema,
  ...(faqs?.length > 0 ? [generateFAQSchema(faqs)] : []),
];
---

<BaseLayout
  title="Precio Examen C1 Cambridge en Madrid — Tasas y Centros [2026]"
  description="¿Cuánto cuesta el examen C1 Advanced en Madrid? Precio oficial 2026, centros examinadores, tasas de inscripción y cómo ahorrar en tu matrícula. Incluye comparativa."
  fullTitle={true}
  canonical="/blog/precio-cambridge-c1-madrid"
  ogType="article"
  schemas={schemas}
>
  <PrecioCambridgeC1MadridPage client:idle />
  <CookieBanner client:idle />
</BaseLayout>
