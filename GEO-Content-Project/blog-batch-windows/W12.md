# W12 — Fixes, Tier B (i): the B2/C1 cluster

**7 fixes: F11, F12, F8 + F13, F14, F15, F16, F17.** You are one of 13 parallel windows.

**No new articles, no research, no pipeline.** Smaller losses than Tier A, but two of these are
**internal-linking decisions worth more than any rewrite**.

## Before you start

1. Read `GEO-Content-Project/blog-batch-windows/_CONTRACT.md` — approved facts (§3), parallel
   safety (§9).
2. **Read `W11.md`'s section "How these pages are built"** — the same structure, the same editing
   rules, the same "read the page before you edit it" warning apply to every fix in this window. It
   is the single most important thing to absorb before touching a file.

Short version: `src/pages/blog/<slug>.astro` is a 23-line wrapper holding only the title/description;
the prose lives in `pages/blog/<Component>Page.tsx` alongside `articleSchema` and a
`faqItems: FAQItem[]` export. Reuse existing class strings, keep the `faqItems` shape exactly, add any
new `<section id>` to the TOC, stay ≤ 60 chars on titles and add `fullTitle={true}` if you change a
title on a page that lacks it. Never run `npm run build` or `verify:design`.

Three of the pages in this window have **no `.astro` wrapper at all** — they are Markdown in the
content collection. Those are marked below and edited completely differently.

## Commit

One commit per fix, explicit paths only:

```bash
cd "/Users/danny/Desktop/backup website Impuls Englisch /March-Impulse-Web-e7ad8740054b4e468e95bc81e5e4c79f17b98c97"
git add pages/blog/<Component>Page.tsx src/pages/blog/<slug>.astro
git commit -m "fix(blog): <slug> — <what changed and the query it targets>"
```

Ledger line: `DONE  W12  F11  diferencia-b2-c1  <sha>  <what changed>`

**Order:** F17 and F13 first (they are linking decisions, minutes each), then F11, F12, F16, F14,
F15.

---

# F17 — `tiempo-preparacion-b2-first` · a linking fix, not a rewrite

- `src/pages/blog/tiempo-preparacion-b2-first.astro` +
  `pages/blog/TiempoPreparacionB2FirstPage.tsx` (491 lines) — **108 imp, 0 clicks**

**The problem is cannibalisation, and the weak page is winning the impression.** Its top query,
`cuanto tiempo se tarda en pasar del b2 first al c1 advanced` (31 imp @ **45.3**), **belongs to F12
`tiempo-b2-a-c1`, which ranks 3.2 for it.** Two of our pages compete and the one at position 45 is
taking the impression.

**The fix: fix the internal linking, do not rewrite.**

1. In this page, wherever the B2→C1 progression is discussed, **link out to
   `/blog/tiempo-b2-a-c1/`** as the answer, and stop competing for it — remove or de-emphasise any
   heading that targets the B2→C1 timing phrase.
2. **Keep this page on `tiempo reading and use of english b2`** (26 imp @ 6.7), where it genuinely
   ranks. That is its real territory: how long each paper takes and how long to prepare *for the
   B2 itself*. Strengthen that, including a per-paper timing section if it is not already explicit.

---

# F13 + F8 — the "C1 pass score" ownership decision

Two pages compete for the same phrases. **The decision is already made — do not re-open it.**

- **F8 owns "C1 pass score": `es-dificil-c1-advanced`** —
  `src/pages/blog/es-dificil-c1-advanced.astro` + `pages/blog/EsDificilC1AdvancedPage.tsx`
  (483 lines) — **377 imp, 0 clicks**. ⚠️ **No `fullTitle` on this one** — if you change the title,
  add `fullTitle={true}` and keep it ≤ 60 chars.
- **F13 defers: `porcentaje-aprobados-c1`** — **⚠️ Markdown, no `.astro` wrapper.** Edit
  `src/content/articles/porcentaje-aprobados-c1.md` — **66 imp, 0 clicks**.

**F8's problem:** the article answers *is it hard*; the searches ask **what score do I need**.

| query | imp | pos |
|---|---|---|
| `cuanto se necesita para aprobar el c1` | 40 | 8.7 |
| `con cuanto se aprueba el c1` | 33 | |
| `nota minima para aprobar c1 cambridge` | 24 | 8.0 |
| `cuantos puntos se necesitan para aprobar el c1` | 22 | |
| `puntuación para aprobar c1 cambridge` | 20 | |

**F8's fix:** add an explicit **C1 pass score** section — the Cambridge English Scale threshold for
the C1 Advanced, the grade bands (Pass at Grade A / B / C), the "B2 achieved" band below, and the
score in the **first 300 characters**. Give it a heading per phrasing (`nota mínima`, `cuántos
puntos`, `con cuánto se aprueba`).

**F13's fix:** `porcentaje-aprobados-c1` also ranks for `con cuanto se aprueba el c1` (49 imp @ 9.8).
It **stops competing**: keep it on pass *rates* and statistics, and **link to
`/blog/es-dificil-c1-advanced/`** for the score. Editing a `.md` means editing front-matter
`contextSections[].content` (HTML strings) — add the link inside the relevant section's HTML and
adjust any heading that targets the score phrasing.

**Link both** to article 20 (`/blog/examen-cambridge-c1-advanced/`, W03 tonight), the new C1 hub.

---

# F11 — `diferencia-b2-c1` · **239 imp, 0 clicks**, ranks 3.9–9.6

- `src/pages/blog/diferencia-b2-c1.astro` + `pages/blog/DiferenciaB2C1Page.tsx` (513 lines)

Ranks page 1 and converts nothing. **The fix is short-form phrasings plus a one-line answer.**

Add the short forms people actually type, as headings or in the H1/intro: **`b2 vs c1`**,
**`b2 o c1`**, **`diferencia entre b2 y c1`**, **`b2 / c1`**. Then **answer the comparison in one
line at the top** — the single sentence that says what actually changes between the two levels
(range and precision under pressure, not more vocabulary).

**Link** to article 20 (`/blog/examen-cambridge-c1-advanced/`, W03) and `/blog/tiempo-b2-a-c1/`.

---

# F12 — `tiempo-b2-a-c1` · **198 imp, 1 click**, and we are in the top 5

- `src/pages/blog/tiempo-b2-a-c1.astro` + `pages/blog/TiempoB2AC1Page.tsx` (467 lines)

| query | imp | pos |
|---|---|---|
| `cuanto tiempo se tarda en pasar del b2 first al c1 advanced` | 46 | **3.2** |
| `cuanto tiempo se tarda en pasar del c1 al c2` | 36 | 4.8 |

Top 5 for both and getting nothing.

**Two fixes:**

1. **Answer both in the first 300 characters** with actual guided-hours figures, then expand. At
   position 3.2 the only thing missing is a reason to click.
2. ⚠️ **The C1→C2 question is unanswered anywhere on the site.** The decision for this batch:
   **F12 owns the C1→C2 *timing*** (it ranks 4.8 for it) — add a real section for it here. **Article
   41** (`/blog/c1-advanced-o-c2-proficiency/`, W10 tonight) owns **the choice** — whether the C2 is
   worth doing at all. Link to it; do not write the "is it worth it" argument here.

Also receives the redirect of attention from F17 above — make sure this page is clearly the better
answer for the B2→C1 timing query.

---

# F16 — `preparar-b2-first-3-meses` · **168 imp, 0 clicks**

- `src/pages/blog/preparar-b2-first-3-meses.astro` + `pages/blog/PrepararB2First3MesesPage.tsx`
  (461 lines) · ⚠️ **no `fullTitle`** — add it if you change the title

| query | imp | pos |
|---|---|---|
| `preparación examen b2` | 55 | 21.6 |
| `como preparar el b2 first` | 21 | 14.6 |
| `como aprobar el first en un mes` | 20 | 12.2 |

⚠️ **The boundary decision is already made:** **this page stays the 3-month plan.** **Article 40**
(`/blog/como-preparar-first-cambridge/`, W09 tonight) owns **"how to prepare" generally** — the
route decision between self-study, academy and intensive. W09 has the same instruction.

**So the fix here is narrow:** strengthen the 3-month plan as *a plan* (week-by-week, what to do
when, what to drop if you are behind), add a section for **`como aprobar el first en un mes`** — the
honest answer being that one month works only from a solid B2 and what to prioritise if that is your
situation — and **link up to article 40** for readers who have not yet chosen a route.

Do **not** broaden this page to own `preparación examen b2`. Article 40 takes that.

---

# F14 — `ejercicios-b2-cambridge` · **276 imp, 2 clicks** · low intent, do not over-invest

- `src/pages/blog/ejercicios-b2-cambridge.astro` + `pages/blog/EjerciciosB2CambridgePage.tsx`
  (660 lines) · ⚠️ **no `fullTitle`**

⚠️ **Its top query is `clgranada.com ejercicios b2` (89 imp @ 8.2)** — people looking for **another
site's material**. That traffic will never convert and chasing it is a waste. **Time-box this fix.**

**Worth adding, and only this:**
- `examenes b2 cambridge resueltos` (48 imp @ 37.5)
- `ejercicios para aprobar el first certificate` (23 imp @ 15.4)

A short section for each phrasing, pointing at genuinely useful material — Cambridge's own free
sample papers are the honest answer here (link to the official source, and to
`/blog/libros-preparar-b2-first/` and `/blog/mejores-libros-b2-first-2026/`).

Do not build a worksheet library. Do not reproduce another site's exercises.

---

# F15 — `consejos-aprobar-writing-b2-first` · **needs a rewrite, not an edit**

- ⚠️ **Markdown, no `.astro` wrapper.** Edit
  `src/content/articles/consejos-aprobar-writing-b2-first.md` — **154 imp, 0 clicks, positions
  44–50**

**The whole writing cluster ranks nowhere:**

| query | imp | pos |
|---|---|---|
| `cómo escribir un essay para el exámen b2` | 30 | 46.5 |
| `writing del b2 de inglés` | 30 | 49.8 |
| `cómo es el writing del b2 de inglés` | 29 | 45.8 |

At positions 44–50 this is not a snippet problem — **the page is nowhere and tips are not what the
query wants.** The brief's recommendation: turn it into a proper **"el writing del B2, parte por
parte"** guide with a **worked essay example**.

**This is the largest single job in your window.** Do it last, and treat it as a rewrite of the
front-matter `contextSections`:

- Part 1 and Part 2 of the B2 First Writing, what each asks for, the word counts, the timing
- the assessment criteria in plain Spanish (Content, Communicative Achievement, Organisation,
  Language) — this is what nobody explains and it is why candidates lose marks
- **a full worked essay**: the prompt, the plan, the finished text, and why it scores
- headings that match the three queries above, verbatim phrasing where it fits

⚠️ **Write the example essay yourself. Do not present it as a real student's work**, and do not
attribute it to anyone. It is a model answer, and saying so is fine.

If this turns out bigger than your remaining budget, **write a `FLAG:` line** saying the rewrite is
scoped but not done, and leave the page untouched rather than half-edited.

---

## Window notes

- **Two of your seven are `.md`, not `.tsx`:** F13 `porcentaje-aprobados-c1` and F15
  `consejos-aprobar-writing-b2-first`. Those live in `src/content/articles/` and are edited as
  front-matter `contextSections[].content` (HTML strings). Everything else is a `.tsx` + `.astro`
  pair.
- **Three decisions in this window are already made** — F17 defers to F12, F13 defers to F8, F16
  defers to article 40. Execute them; do not re-derive them.
- After each edit, `grep` for the phrasing you added and confirm it landed in a heading or the first
  300 characters. That is the verification.
- Ledger line per fix: `DONE  W12  F11  <slug>  <sha>  <what changed>` — or `NOOP` / `FLAG` with a
  reason.
