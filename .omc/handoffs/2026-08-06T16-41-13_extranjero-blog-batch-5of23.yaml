# Handoff — 2026-08-06T16-41-13
goal: "Move seo-blog-writer research off Apify onto Firecrawl, then write the 22-article extranjero blog cluster plus its hub."
now: "5 of 23 articles written, built, committed and pushed. 17 articles + hub remain."
done_this_session:
  - "Firecrawl swap: new scripts/lib/firecrawl.js wraps the authenticated CLI, same export surface as apify.js"
  - "research.js line 10+113 now import/call firecrawl; Apify kept as fallback that logs loudly"
  - "Proved swap end-to-end: 38 Firecrawl scrapes across 8 runs, ZERO Apify calls; forced-failure test confirms fallback fires"
  - "Wrote+shipped 5 articles: becas bachillerato, becas inmersion, vivir en Irlanda, Malta, internados Inglaterra"
  - "Built gate-article.sh (now in scripts/seo/), verified it passes known-good AND catches injected defects"
  - "Pushed 6 commits to origin/seo/extranjero-hub (ed608e0..8058e9e)"
blockers:
  - "6.750 EUR inclusions still unknown — Danny said he'd tell me, then pivoted to the Apify question. Blocks the pricing article only."
decisions:
  - "Firecrawl via CLI not SDK: no FIRECRAWL_API_KEY exists on this machine, but firecrawl-cli is authenticated via stored credentials"
  - "Apify kept wired as fallback rather than deleted, so a Firecrawl outage degrades instead of failing the batch"
  - "No Impulse price published in any of the 5 articles; third-party boarding-school fees ARE published as market ranges"
  - "England/Scotland articles state twice that Impulse does not place students there (offer-honesty rule §4)"
  - "metaTitleMaxLength cut 60 -> 41 to match utils/buildPageTitle.ts, which hard-cuts mid-word"
findings:
  - "cmsProfile was NEVER set in the brand config — converter was built and wired, switch never flipped; assemble emitted generic front-matter"
  - "rotate-images.js APPENDED its block instead of replacing, leaving two contradictory image lists per article"
  - "PRE-EXISTING: 25 of 98 blog pages already ship mid-word truncated titles. Not caused by this work."
  - "academyImages.ts contains logos, duplicates and dnny-tour-of-ireland.jpg — rotation gave that Ireland photo to the US article"
  - "Plan's own §6 assigns articles 12 and 18 terms that §1 says the pillar owns; §9 forbids it"
  - "PR #15 is MERGED (pillar pages). The 6 new commits have NO open PR."
next:
  - "Resume writing at rotation index 8 — indices 0-7 are consumed"
  - "3 run dirs already have paid research + rotated images: EE.UU. (idx5), campamentos Inglaterra (idx6), campamento Irlanda (idx7) — do NOT re-run prewrite"
  - "Write articles 12 and 18 to the NON-owned half of their term pair, link pillar for the commercial query"
  - "After the batch: python3 scripts/seo/gen_blog_directory.py, then npm run build, confirm entry count moved by 23"
  - "Open a fresh PR once the batch is complete (PR #15 is already merged)"
  - "Get the 6.750 EUR inclusions from Danny before writing the pricing article"
files:
  modified:
    - "~/.claude/skills/seo-blog-writer/scripts/lib/firecrawl.js (new)"
    - "~/.claude/skills/seo-blog-writer/scripts/lib/research.js"
    - "~/.claude/skills/seo-blog-writer/scripts/lib/meta.js"
    - "~/.claude/skills/seo-blog-writer/scripts/rotate-images.js"
    - "~/.claude/skills/seo-blog-writer/brands/impulse-english.brand.json"
    - "~/.claude/skills/seo-blog-writer/SKILL.md + .env.example"
    - "March-Impulse-Web-.../src/content/articles/ (5 new .md)"
    - "March-Impulse-Web-.../scripts/seo/gate-article.sh (new)"
    - "EXTRANJERO-BLOG-CLUSTER-PLAN.md"
  read:
    - "GEO-Content-Project/extranjero-blog-briefs.md"
    - "March-Impulse-Web-.../utils/buildPageTitle.ts"
    - "March-Impulse-Web-.../components/PAAArticlePage.tsx"
    - "March-Impulse-Web-.../src/data/academyImages.ts + extranjeroImages.ts"
errors: []
git_branch: "seo/extranjero-hub"
